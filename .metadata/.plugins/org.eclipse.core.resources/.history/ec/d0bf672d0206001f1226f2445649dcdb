package chap05_array;

import java.util.Random;

public class _04_ArrayExample {
	// 1. 자바에서 랜던 값 생성방법 1: Random 클래스 사용
	// Random.nextInt(바운드) : 0 ~ 바운드 - 1 까지의 숫자 중 랜덤한 값 생성
	Random random = new Random();
	random.nexInt(10);
}
