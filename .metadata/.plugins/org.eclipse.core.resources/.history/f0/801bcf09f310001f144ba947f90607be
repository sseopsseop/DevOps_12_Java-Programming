package chap20_enum;

import chap20_enum.enums.DefaultMonth;

public class _02_DefaultEnum {
	public static void main(String[] args) {
		// 1. Enum은 객체처럼 사용
		// DEfaultMonth.JAN에 해당하는 값의 DefaultMonth 객체를 하나 생성
		DefaultMonth defaultMonth = DefaultMonth.JAN;
	}
}
